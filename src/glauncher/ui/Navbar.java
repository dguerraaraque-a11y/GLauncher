package glauncher.ui;

import javafx.scene.layout.HBox;

public class Navbar extends HBox {
    // Esta clase no se está utilizando actualmente (la lógica está en MainView).
    // Se deja vacía para permitir la compilación.
    public Navbar() {}

    // Constructor de compatibilidad para que MainLayout.java (obsoleto) no falle
    public Navbar(Object ignored) {}
}
